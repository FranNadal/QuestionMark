<!DOCTYPE html>
<html>
<head>
    <title>Perfil de Usuario</title>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="https://www.w3schools.com/lib/w3-theme-purple.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto">
    <style>
        body {
            background: linear-gradient(to bottom, #f7f3fc, #ffffff);
            font-family: 'Roboto', sans-serif;
        }
        .perfil-container {
            max-width: 800px;
            margin: auto;
        }
        .perfil-img {
            width: 120px;
            height: 120px;
            object-fit: cover;
        }
    </style>
</head>
<body class="w3-light-grey">
{{#datos}}
<div class="w3-container w3-padding-32 perfil-container">
    <div class="w3-card w3-white w3-round-large w3-padding-large">
        <div class="w3-center">
            <img src="{{foto_perfil}}" alt="Foto de perfil" class="w3-circle perfil-img w3-border w3-border-purple">
            <h2 class="w3-text-purple">{{nombre_completo}}</h2>
            <p>{{nombre_usuario}}</p>
            <span class="w3-tag w3-theme w3-round">{{rol}}</span>
        </div>

        <hr class="w3-border-purple">

        <div class="w3-row-padding w3-margin-top">
            <div class="w3-half w3-margin-bottom">
                <b>Año de nacimiento:</b>
                <p>{{ano_nacimiento}}</p>
            </div>
            <div class="w3-half w3-margin-bottom">
                <b>Sexo:</b>
                <p>{{sexo}}</p>
            </div>
            <div class="w3-half w3-margin-bottom">
                <b>País:</b>
                <p>{{pais}}</p>
            </div>
            <div class="w3-half w3-margin-bottom">
                <b>Ciudad:</b>
                <p>{{ciudad}}</p>
            </div>
            <div class="w3-half w3-margin-bottom">
                <b>Email:</b>
                <p>{{email}}</p>
            </div>
            <div class="w3-half w3-margin-bottom">
                <b>Fecha de registro:</b>
                <p>{{fecha_registro}}</p>
            </div>
        </div>

        <div class="w3-center w3-margin-top">
            <button class="w3-button w3-purple w3-round-large">Editar perfil</button>
        </div>
    </div>
</div>
{{/datos}}
</body>
</html>
